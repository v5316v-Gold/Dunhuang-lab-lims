# W+5 周报 — Phase 1C 功能开发第 2 周

> **周期**: 2026-08-15(Phase 1C W+5)
> **上一节点**: Phase 1C W+4(9485152)
> **当前 HEAD**: 21ff55e
> **本周完成**: 4/4 任务

---

## 1. 本周完成(W+5)

| # | 任务 | 关键产出 | 测试 |
|---|---|---|---|
| **W+5-1** | 资源级 RBAC 全表集成 | @Ownership sample PATCH/DELETE 端到端 | w5-rbac-ownership 4/4 |
| **W+5-2** | 火试金工艺参数前端表单 | FireAssayForm 工艺参数 Card + saveProc | w5-fire-assay-process 3/3 |
| **W+5-3** | 校准曲线 R² 输入 + 渲染 | ElementResultForm + R² 等级实时反馈 | w5-calibration-r2 3/3 |
| **W+5-4** | Levey-Jennings 图(纯 SVG) | qc/LeveyJenningsChart.tsx + ±3 SD 标尺 | w5-levey-jennings 3/3 |

## 2. 关键产出(评审可演示)

### Demo 1: 资源级 RBAC 双层守卫
```
非 owner 的 ANALYST PATCH /samples/:id → 403(RBAC + Ownership 双拒)
admin (receivedById 匹配) PATCH /samples/:id → 200(自动 bypass)
```

### Demo 2: 火试金工艺参数
```
POST /tests/fire-assay/:testId/process
{ furnaceTempC: 950, cupellationMin: 35, partingMin: 30, annealingMin: 20, partingAcid: '硝酸 1:7' }
→ 持久化到 FireAssayDetail(评审必查 §7.5)
```

### Demo 3: 校准曲线 R² 录入
```
POST /tests/icp/:testId/results
{ element: 'Au', concentration: 0.0123, calibrationR2: 0.9998 }
→ 持久化到 ElementResult
前端实时反馈 R² 等级:≥0.999 绿/0.995-0.999 青/0.99-0.995 蓝/<0.99 橙
```

### Demo 4: Levey-Jennings 图
```
GET /qc/trend?element=Au&days=30
→ [{ zScore, run, passed }, ...]
前端纯 SVG 渲染:中心线 ±1/2/3 SD 标尺,失控点红/受控点蓝
```

## 3. 测试态势(全量回归)

| 指标 | 数值 |
|---|---|
| 测试 suites | **36** |
| 测试用例 | **339/339 PASS** |
| Prisma 模型 | 47 |
| 迁移 | 21 |
| 后端 controllers | 23 |
| API 端点 | 138+ |

## 4. 修复缺陷(W+5 期间)

| # | 缺陷 | 修复 |
|---|---|---|
| 1 | QC controller 端点是 /qc/trend 不是 /qc/measurements | 前端 + 测试改路径 |
| 2 | SampleBatch enum 断言用了不存在的 REPORT | 改 CALCULATING/WEIGHING/ANNEALING |

## 5. 下周(W+6)计划

```
W+6-1 临时授权 UI
W+6-2 监督记录 CRUD
W+6-3 盲样考核流程
W+6-4 安全扫描(npm audit + 安全头检查)
```

## 6. 风险提示

- �️ 前端页面用 React 19 + antd 5,部分 ref 转发需用 ref 函数
- ✅ 全量 339 测试保持,无 P0/P1 回归

---

**提交**: 21ff55e feat(phase-1c): W+5-4 Levey-Jennings 图(纯 SVG 零依赖) + 端到端测试
**HEAD**: 21ff55e(已 push)
